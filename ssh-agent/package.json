{
  "name": "ssh-agent",
  "version": "1.0.0",
  "description": "Inits ssh-agent with ssh-private-key and exports SSH_AUTH_SOCK.",
  "main": "index.js",
  "scripts": {
    "lint": "eslint **.js",
    "package": "ncc build index.js -o dist && ncc build cleanup.js -o dist/cleanup"
  },
  "author": "Kraussmaffei Technologies",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.6"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.22.3",
    "eslint": "^7.20.0"
  }
}
